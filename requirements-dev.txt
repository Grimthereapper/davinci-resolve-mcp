# Development + test dependencies. Not needed to run the MCP server.
#   ./venv/bin/python -m pip install -r requirements-dev.txt

-r requirements.txt

# Test runner. The suite is a mix of unittest.TestCase and bare test_ functions;
# pytest collects both, which is why CI can run the whole tree rather than the
# handful of modules `python -m unittest` was invoked with by name.
pytest>=8

# Linter. No lint config existed before, so ruff.toml starts deliberately narrow
# (see that file) — a full ruleset on 122 files would be noise, not signal.
ruff>=0.6

# requests — used only by tests/test_custom_timeline.py and
# tests/test_improvements.py, which drive a live HTTP endpoint. Not imported
# anywhere under src/.
requests>=2.31

# numpy — test-time need for tests/test_colorimetry.py and tests/test_image_qc.py.
# NOTE: src/utils/colorimetry.py imports numpy unguarded at module scope, so it is
# really a runtime dependency of that module, not a dev one. Every other numpy use
# under src/ is either lazy (inside a function) or guarded by try/except, matching
# the comment in image_qc.py that numpy "is not a hard requirement". Either move
# numpy into requirements.txt or make colorimetry.py guard it like image_qc.py —
# until then a clean install raises ImportError on `import src.utils.colorimetry`.
numpy>=1.24
